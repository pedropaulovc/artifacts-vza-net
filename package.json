{
  "name": "artifacts-vza-net",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy:production": "wrangler deploy --config wrangler.production.jsonc",
    "deploy:ppe": "wrangler deploy --config wrangler.ppe.jsonc"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.30.0"
  }
}
